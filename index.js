/**
 * @format
 */

import {AppRegistry} from 'react-native';

import {name as appName} from './app.json';
import { ServicesApp } from './src/ServicesApp';

AppRegistry.registerComponent(appName, () => ServicesApp);
